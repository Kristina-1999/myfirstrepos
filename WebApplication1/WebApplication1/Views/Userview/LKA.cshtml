@using PagedList.Mvc;
@model PagedList.IPagedList<WebApplication1.Models.User>


<link href="Site.css" rel="stylesheet" type="text/css" />
@{
    ViewBag.Title = "User";
}

<hr />
<h4>Личный кабинет администратора</h4>
<hr />
<table class="table">
    <tr>
        <td>
            Username
        </td>
        <td>
            E-Mail
        </td>
        <td>
            Password
        </td>

    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Username)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Password)
            </td>
            <td>
                @Html.ActionLink("Удалить", "DeleteUser", new { id = item.id })
            </td>
        </tr>
    }

</table>
<br /><br /><br />
Страница @(Model.PageCount < Model.PageNumber ? 0 : Model.PageNumber) из @Model.PageCount

@Html.PagedListPager(Model, page => Url.Action("LKA", new { page }))
<br />
<br />
@Html.ActionLink("Редактировать группы", "GroupsPage")
<br />
<br />
@Html.ActionLink("Редактировать товары", "ProductsPage")
<br />
<br />
@Html.ActionLink("Редактировать заказы", "OrdersPage")
<br />
<br />
@Html.ActionLink("Выход", "LoggingOff")